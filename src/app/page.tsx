export default function HomePage() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <a href="/admin" className="text-[14px] font-medium" style={{ color: 'var(--ink)' }}>
        Go to Admin →
      </a>
    </div>
  );
}
